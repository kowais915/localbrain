{
  "name": "localbrain",
  "version": "0.3.1",
  "description": "Give any app a free, private AI that runs on the user's own machine — no API key, no bill, works offline. One command, and it downloads its own brain.",
  "keywords": [
    "ai",
    "llm",
    "local",
    "offline",
    "privacy",
    "openai-compatible",
    "gguf",
    "local-first",
    "free"
  ],
  "homepage": "https://github.com/kowais915/localbrain#readme",
  "bugs": {
    "url": "https://github.com/kowais915/localbrain/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kowais915/localbrain.git",
    "directory": "packages/cli"
  },
  "license": "MIT",
  "type": "module",
  "bin": {
    "localbrain": "./dist/cli.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "MODELS.md"
  ],
  "engines": {
    "node": ">=18.17"
  },
  "scripts": {
    "build": "tsup",
    "build:tsc": "tsc --build",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "node-llama-cpp": "^3.0.0"
  },
  "devDependencies": {
    "@localbrain/adapters": "*",
    "@localbrain/detection": "*",
    "localbrain-client": "*",
    "@localbrain/runtime": "*",
    "tsup": "^8.0.0"
  },
  "//": "This CLI package ('localbrain') bundles the internal @localbrain/* workspace packages AND localbrain-client via tsup (noExternal); node-llama-cpp is kept external so its prebuilt binaries install normally. Apps should install the lightweight 'localbrain-client' package instead of this one — it has zero native deps.",
  "publishConfig": {
    "access": "public"
  }
}
